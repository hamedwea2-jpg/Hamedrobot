# برنامه تست زنده — بعد از `/login`

ربات لاگین بالا است: **@Hardkso_bot** → `/start` → `/login` → شماره → کد (با فاصله: `1 2 3 4 5`)
بعد از ساخت سشن، یوزربات **خودکار** استارت می‌شود (لازم نیست ری‌استارت کنی).

---

## ۱) تست‌های خودکار (بدون سشن) — همین حالا قابل اجرا

```bash
cd /home/user/userbot
python3 tests/selftest.py     # ۴۵ تست رفتاری روی کد واقعی userbot.py
python3 tests/check_py310.py  # سازگاری با Python 3.10/3.11
```

این‌ها کلاینت تلگرام را شبیه‌سازی می‌کنند ولی **خودِ هندلرهای `userbot.py`** را
اجرا می‌کنند. آخرین اجرا: **45 PASS / 0 FAIL**.

---

## ۲) تست زنده بعد از ورود

| # | چه‌کار کنی | چه انتظار داشته باشی |
|---|---|---|
| ۱ | در پی‌وی یوزربات: `.doctor` | 🟢 تاخیر ایونت‌لوپ زیر ۲۵۰ms، صف ۰ |
| ۲ | `.stats` | صف `0 / 500`، قدیمی‌ترین در صف `0s` |
| ۳ | `.pmtest` | وضعیت پی‌وی + متن فعلی را نشان می‌دهد |
| ۴ | `.pm on` و `.pmadmins on` | حالا از اکانت خودت پی‌وی بزن → باید جواب بگیری |
| ۵ | با یک اکانت دیگر پی‌وی بفرست | بین ۱.۵ تا ۴ ثانیه بعد جواب می‌گیرد، بار دوم نه |
| ۶ | داخل یک گروه خودت `.addchat` | گروه فعال می‌شود |
| ۷ | چند نفر در گروه پیام بدهند | هر نفر **یک** ری‌اکشن در ۲۴ ساعت |
| ۸ | یک نفر ۲۰ پیام پشت‌سرهم بدهد | فقط **۱** ری‌اکشن؛ با `.doctor` ببین «ادغام‌شده» بالا می‌رود |
| ۹ | `.workers` | ظرفیت روزانه + زمان رسیدن به همهٔ صف |
| ۱۰ | `.workers 3` | ظرفیت سه برابر، در لاگ `[worker] #2 started` |
| ۱۱ | `.stats` | «عضوهای در انتظار» و «⏳ زمان رسیدن به همهٔ صف» |
| ۱۲ | `.queue clear` | صف صفر می‌شود |

---

## ۳) تست فشار (مهم‌ترین تست برای باگ «هنگ»)

```
.maxage 10
.maxqueue 500
.doctor        ← قبل
... ۱۰ دقیقه فعالیت سنگین در گروه‌ها ...
.doctor        ← بعد
```

انتظار:
- «صف» هیچ‌وقت از `500` بالاتر نرود
- «تاخیر ایونت‌لوپ» سبز بماند
- «بیات ردشده» و «ادغام‌شده» رشد کنند (یعنی مکانیزم‌های محافظ کار می‌کنند)

---

## ۴) ظرفیت واقعی — این را جدی بگیر

**صف نامحدود است؛ هیچ عضوی جا نمی‌ماند.** ولی سرعت ارسال محدود است:

```
ظرفیت روزانه = تعداد کارگر × ۸۶۴۰۰ ÷ میانگین فاصله
```

| فاصله | ۱ کارگر | ۳ کارگر | ۵ کارگر |
|---|---|---|---|
| ۲۰–۳۰s | ۳٬۴۵۶ | ۱۰٬۳۶۸ | ۱۷٬۲۸۰ |
| ۸–۱۵s | ۷٬۵۱۳ | ۲۲٬۵۳۹ | ۳۷٬۵۶۵ |
| ۳–۵s | ۲۱٬۶۰۰ | ۶۴٬۸۰۰ | ۱۰۸٬۰۰۰ |

برای ۳۰٬۳۶۵ عضو با تنظیم فعلی (۲۰–۳۰s، ۱ کارگر) **~۸.۸ روز** طول می‌کشد تا
به همه برسی — یک‌بار، و بعد از آن هر روز فقط عضوهای جدید.

سریع‌تر می‌خواهی؟
```
.workers 3        ← ظرفیت سه برابر، فاصله هر کارگر همان ۲۰-۳۰s می‌ماند
.delay 8 15       ← ظرفیت ~۲ برابر، ریسک بیشتر
```

`.doctor` و `.workers` هر دو «زمان رسیدن به همهٔ صف» را نشان می‌دهند.
اگر صف از ظرفیت یک روز بزرگ‌تر شود، `.doctor` هشدار می‌دهد.

⚠️ `.delay` زیر ۳ ثانیه پذیرفته نمی‌شود. هرچه کارگر بیشتر، احتمال FloodWait
بالاتر — با ۲ یا ۳ شروع کن و لاگ را نگاه کن (`[flood]` یعنی تلگرام محدود کرده).

---

## ۵) نکات ایمنی

- توکن ربات لاگین و `api_hash` در `config.json` لو رفته‌اند (لینک دانلود عمومی بود).
  در @BotFather توکن را **revoke** کن و در my.telegram.org یک `api_hash` جدید بساز.
- `data/session.txt` = دسترسی کامل به اکانت. هرگز share نکن.
- این محیط یک sandbox موقت است. برای استفاده دائم روی سرور خودت ببر:
  `scp -r userbot user@host:/opt/hc-userbot && bash deploy.sh`
